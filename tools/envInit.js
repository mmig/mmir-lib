
/**
 * sets up the runtime environment:
 * 
 * loads the Corodva library if necessary.
 */
define(['env', 'module'], function(env, module) {
	
	var isBrowserEnv = env.isBrowserEnv;

//	//DISABLED: using new mechanism for loading Cordova 3.x
//	if( ! isBrowserEnv){
//		require(['cordova']);
//	}
});
